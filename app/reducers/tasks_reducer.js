import {
  SET_LOADING,
  SET_ERROR,
  GET_TASKS,
  CREATE_TASK,
  EDIT_TASK,
  DELETE_TASK,
  DELETE_ALL_TASKS,
} from "../actions";

const tasks_reducer = (state, action) => {
  switch (action.type) {
    case GET_TASKS:

    default:
      break;
  }
};
